using UnityEngine;
using System.Collections;

public class testBall : MonoBehaviour {

	// Use this for initialization
	void Start ()
	{
	    //var offset = Vector3.Normalize(transform.position - new Vector3(0,0, 0));
	    //gameObject.StartAutoTween(new Vector3(0,0,0), transform.position, 6, EasingType.outElasticBig, 3f, () => Floating.AttachToGameObject(gameObject, offset));

//		var positions = Position.PositionsFromRadial(20f);
//		gameObject.transform.localPosition = positions.ClosedPosition;

	}

    // Update is called once per frame
	void Update () {
	
	}

    void OnMouseDown()
    {

    }
}
